作用
	Java虚拟机就是二进制字节码的运行环境
特点

	一次编译到处运行的跨平台特性，一份编译好的字节码文件可以在不同操作系统下的JVM运行(本质是字节码的跨平台)
	自动内存管理
	自动垃圾回收

编译后的字节码文件格式主要分为两部分：常量池和方法字节码。

运行Java程序的过程：
	前端编译：把Java源码文件（.java）编译成Class文件(.class)的过程。

	后端编译：通过Java虚拟机（JVM）内置的即时编译器（Just In Time Compiler，JIT编译器）；在运行时把Class文件字节码编译成本地机器码（机器指令）。
 